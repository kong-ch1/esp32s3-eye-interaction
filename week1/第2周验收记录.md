# 第2周 · 真机验收记录

**日期**：2026-09-18 11:00
**设备**：ESP32-S3-EYE（板载 QMA6100P 三轴加速度计，WiFi IP `10.1.41.160`）
**设备标识**：`device_id = team01-esp32s3eye`
**链路**：网页 → 服务器 `10.1.41.18:8000` → 板子 → 板子执行新采集 → 服务器入库 → 网页
**原始输出**：`debug_logs/_w2_acceptance_20260918_110015.txt`（由 `tools/cmd_acceptance.py --save` 生成）

---

## 一、验收目标

第 1 周只能做到「板子推、网页看」。第 2 周要证明：

1. 网页能**让设备再采一次**，而不是只能看历史；
2. 这条链路的**每一段都有证据**：服务器受理 / 指令下发 / 设备接收 / 执行完成 / 超时；
3. 新采的数据能**精确关联**到触发它的那次请求，不靠时间窗口猜。

---

## 二、证据链实测（真机，非模拟）

指令 `CMD-20260918-0005`，参数：采 12 条、间隔 100ms。

| 阶段 | 服务器侧记录时刻 | 相对上一步 |
|---|---|---|
| ① 服务器受理 `issued` | 2026-09-18 **11:00:11.560** | — |
| ② 指令下发 `delivered` | 2026-09-18 **11:00:12.453** | +893 ms |
| ③ 设备接收 `received` | 2026-09-18 **11:00:12.879** | +426 ms |
| ④ 执行完成 `done` | 2026-09-18 **11:00:15.664** | +2785 ms |
| 端到端 | | **4104 ms** |

**各段耗时说明**：

- **受理 → 下发 893 ms**：板子每 1 秒才上报一次，指令搭在下一次上报的响应里，所以这段天然在 0~1000 ms 之间。这是本方案**有意为之的取舍**（见设计文档第 2 节），换来的是板子不必开监听端口、不必引入 MQTT。
- **下发 → 设备接收 426 ms**：一次 HTTP 往返（板子收到指令后立刻回报 `received`），含无线抖动。
- **接收 → 完成 2785 ms**：12 条 × 100 ms 间隔 = 1200 ms，加上 12 次 HTTP 上报的往返开销。

这条链上**每一段都是可测量的**，不是"点了一下，等一会儿，出来了"。

---

## 三、「刷新存储记录」与「触发新采集」的区别

这是本单元最容易含糊过去的地方，所以用**同一时刻的两次操作**直接对比：

| 操作 | 结果 |
|---|---|
| **刷新**（再次 `GET /api/latest`） | 还是同一条记录 `id=21906`、`seq=4`；库里仍是 **20883** 条；耗时 8 ms |
| **点「重新采集」**（`POST /api/command`） | 新增 **13** 条记录（`id 21908~21920`），全部带 `request_id=CMD-20260918-0005`；端到端 4104 ms |

刷新**一条都不会多**，下发指令才会多。这不是界面上的文案，是数据库里的行数。

---

## 四、数据层如何区分新采集与历史记录

新增两个字段，让"这条数据是谁触发的"成为**数据库里的直接关联**，而不是靠时间推断：

| 字段 | 定时上报 | 本次指令触发 |
|---|---|---|
| `trigger` | `'timer'` | `'command'` |
| `request_id` | `NULL` | `CMD-20260918-0005` |
| `cmd_state` | `NULL` | 首条 `received`、末条 `done`、中间 `NULL` |

本次指令触发的 13 条记录（首条与末条的回执标记可见）：

| id | trigger | cmd_state | ax | ay | az | 服务器时间 |
|---|---|---|---|---|---|---|
| 21908 | command | **received** | -0.0073 | -0.1238 | -0.9843 | 11:00:12 |
| 21909 | command | — | 0.0054 | -0.1296 | -0.9779 | 11:00:13 |
| 21910 | command | — | -0.0083 | -0.1199 | -1.0015 | 11:00:13 |
| 21914 | command | — | 0.0005 | -0.1238 | -0.9929 | 11:00:14 |
| 21918 | command | — | 0.0015 | -0.1199 | -0.9929 | 11:00:15 |
| 21919 | command | — | 0.0034 | -0.1218 | -0.9908 | 11:00:15 |
| 21920 | command | **done** | -0.0073 | -0.1277 | -1.0036 | 11:00:15 |

（完整 13 条见原始输出文件）

**数据合理性核对**：13 条的模长 √(ax²+ay²+az²) 均落在 0.99~1.01 g，符合板子平放静止的物理预期 —— 说明这批数据是**真实采集**，不是补发的旧值。

---

## 五、超时分支（服务器主动判定并留证）

超时不是"查的时候顺手算"，而是服务器后台看门狗**每秒扫一次并落库**，
所以超时这件事有服务器侧的时间戳。

实测（`tools/cmd_selftest.py --timeout-test`，发指令后故意不去取）：

```
request_id : CMD-20260918-0004
status     : timeout
created_at : 2026-09-18 10:51:42.258
timeout    : 2026-09-18 10:52:12.712（阶段=accept）
total_ms   : 30454
```

`timeout_stage` 区分两种完全不同的故障，验收时能一眼看出问题出在哪一段：

- **`accept`** —— 指令发出去了，板子一直没来取 → 板子掉线 / 重启 / 没在 上报
- **`exec`** —— 板子取走了，但没执行完 → 执行中崩了 / 上报通道断了

---

## 六、真实浏览器实测（2026-09-20 补测）

前面几节的验收都是用命令行脚本做的。网页部分此前只做过**静态**检查
（JS 语法、元素 id 一致性、接口连通性）和**离线**逻辑验证 —— 那些都证明不了
"在真实浏览器里点一下按钮，界面真的会动"。这一节补上。

**工具**：`tools/browser_test.py` —— 用 **Chrome 153 无头模式 + DevTools Protocol**
真实打开页面、真实点击按钮、读取真实 DOM。不依赖任何第三方库
（CDP 走 WebSocket，标准库没有客户端，脚本里手写了一个极简实现）。

**实测输出**（原样摘自运行日志，完整见 `debug_logs/_w2_browser_test.txt`）：

```
[1] 页面渲染实时数据
    device: team01-esp32s3eye     状态: 实时更新中
    ax=0.005g  ay=-0.225g  az=-0.973g   |a|=0.998g
    服务器时间 2026-09-20 13:42:19   数据年龄 0.6 秒   来源IP 10.1.41.160

[2] 点击「重新采集」按钮（8 条 · 间隔 120ms）

[3] 时间线四阶段依次点亮
    ① 服务器受理   已完成  13:42:20.935
    ② 指令已下发   已完成  13:42:21.159
    ③ 设备已接收   已完成  13:42:21.366
    ④ 执行完成     已完成  13:42:23.418

[4] 页面最终文字
    指令 CMD-20260920-0002 · 状态 done
    本次新采集入库 9 条（id 22256 ~ 22264）
    端到端耗时 2483 ms
    下发前库里 21231 条 → 下发后库里 21241 条（+10）

[5] ✓ 真实浏览器里点击按钮 → 四阶段依次点亮 → 全部完成
```

**截图**：`debug_logs/_w2_browser_1_before.png`（点击前）、
`debug_logs/_w2_browser_2_after.png`（点击后，可见时间线四段全绿、
底部「下发前 21231 条 → 下发后 21241 条（+10）」、表格里带「指令」标记的行已高亮）。

**判定依据是 DOM 上的 class**（`done` / `cur` / `bad`），不是去匹配界面文案 ——
文案会变，class 是渲染逻辑真正吐出来的东西。

---

## 七、按任务卡补齐（2026-09-23）

对照课程任务卡逐条核验后，发现第 2 周有两项**漏做或只做了一半**，另有一项要求的交付物缺失。
这一节是补齐后的验收记录 —— 原始输出见 `debug_logs/`。

### 7.1 暂停周期上报（任务卡「后2学时」与「当堂验证」两处都要求）

任务卡原话要求**暂停周期上报、保持命令通道**，然后点采集、核对新观测与 `request_id`。
**这一条之前完全没做。**

难点在于：板子只做 POST、不监听端口，指令全靠搭在它**自己上报请求的响应体**里下发 ——
上报一停，通道就断了。任务卡那句"保持命令通道"正是指这个矛盾。

**解法**：暂停期间把「上报数据」换成「心跳」。

| 状态 | 板子发的请求 | 写库 | 取指令 |
|---|---|---|---|
| 正常 | `POST /api/data` | ✅ 写一条记录 | ✅ |
| 已暂停 | `POST /api/poll` | ❌ 一条都不写 | ✅ |

通道不断，而库里一条新传感记录都不会有。实测（`tools/pause_acceptance.py`，全自动）：

| 步骤 | 实测结果 |
|---|---|
| ① 基线 | 3 秒内库内 33783 → 33785（**+2**）—— 周期上报正常，作为对照 |
| ② 下发 `pause` | `CMD-20260923-0017` → `done`，四段 419 / 1459 / 884 ms，端到端 2762 ms |
| ③ 暂停生效（观察 6 秒） | 库内 33786 → 33786（**+0**）；板子自报 `paused=True`，**2.01 秒前刚发过心跳** |
| ④ 暂停中点采集 | `CMD-20260923-0018` → `done`，新采集 8 条，端到端 7220 ms |
| ⑤ **关键断言** | 这 8 条 **100% 是 `trigger='command'`，0 条 `timer`** |
| ⑥ 下发 `resume` | `CMD-20260923-0019` → `done`，3 秒内恢复 +2 条 |

第 ⑤ 步就是任务卡要的那个证明：**周期上报停着的时候，数据库里任何新记录都不可能来自
"页面翻出来的旧值"**。这比在界面上比数字硬得多。

> 附带修掉一个会造成"冤假错案"的隐患：暂停期间读数不再增长，数据年龄必然变大。
> 原来的逻辑只看年龄，会把它显示成「未更新 · 已 N 秒」—— 既冤枉了设备，
> 又**掩盖了"暂停生效"这个事实**。现在三态分开显示：
> 暂停中（心跳 N 秒前）/ 正在恢复 / 真未更新。

### 7.2 失败态：从死代码变成可复现的真实路径

任务卡要求 Web 显示「完成、**失败**或超时」。此前 `failed` 这个状态在状态机里定义了、
网页也能渲染，但**没有任何代码会设置它** —— 是死代码。

**为什么一直没验**：失败在真实环境里很难复现。拔网线的话，设备连"我失败了"都报不上来；
把服务器弄挂，整个链路一起断，分不清是谁的问题。

**做法**：给服务器加一个**受限的故障注入口**（`--fault-injection` 才生效，默认关闭），
让接下来 N 次「指令采集上报」返回 503。关键在于它**只拦样本、不拦回执**：

```
设备 → 样本上报 ×N   ← 被注入的故障挡住（503）
设备 → received 回执  ← 照常送达
设备 → failed 回执    ← 照常送达，带上原因
```

回执信道不被切断，测试才有确定的终点，而不是一路等到超时。

真机实测（`tools/fail_acceptance.py`，板子是真板子）：

| 步骤 | 实测结果 |
|---|---|
| 注入故障 | 接下来 6 条指令采集上报返回 503 |
| 下发采集 | `CMD-20260923-0008`：`issued`(0.0s) → `delivered`(0.8s) → `received`(2.4s) → **`failed`(11.1s)** |
| 设备备注 | **「6 条全部上报失败（网络或服务器不可达）」** |
| 库内核对 | 该 `request_id` 只有 **2 条**记录：1 条 `received` + 1 条 `failed`，**样本 0 条** |
| `timeout_ms` | `None` —— 是失败不是超时，两者落在不同的列 |
| **对照实验** | 撤掉故障后重采：`CMD-20260923-0009` → `done`，新采集 6 条，端到端 10587 ms |

最后那行对照是刻意加的：**没有它，就没法排除"板子本来就有病"这个解释。**
同一块板子、同一条链路，注入故障就失败、撤掉就成功 —— `failed` 这条路径因此是可信的。

补上失败态后还发现两处措辞/统计上的问题，一并修掉：

- 时间线第 4 步原先叫「执行完成」，失败时会显示成「**执行完成 · 执行失败（设备回报）**」，
  自相矛盾。改名为「**④ 执行结果**」—— 这一步本来就有三种结局。
- `failed` 的指令在接口里没有总耗时（`total_ms` 只算了 `done` 和 `timeout`），
  看起来像半条记录。已把 `failed_ms` 纳入。

#### 7.2.1 过程中发现的一个真问题：重复的终态回执

第一次真机验证时，库里出现了 **3 条一模一样的 `failed` 记录**（`seq` 60 / 61 / 62）。
查下来是：

- 服务器已经入库并返回 200，
- 但板子那边 `esp_http_client_fetch_headers()` 超时判为失败（板子自己的串口日志记了
  `上报失败 HTTP -1`），
- 于是板子按设计**重试**了结论上报 —— 而服务器每次都老老实实又插了一条。

上报是「发出去」和「收到回应」两件事，中间断了设备就分不清服务器处理没处理
（经典的两将军问题）。所以**设备必须重试，而服务器必须对重试免疫**。

修法：同一 `request_id` + 同一终态 `cmd_state` 已存在时，返回
`{"ok":true,"duplicate":true}` 且**不再入库**。判定很窄，只覆盖 `done` / `failed` ——
终态在状态机里本来就冻结了，第二条不携带任何新信息。

> 这类 bug 的产物是「看起来像证据、其实是错的数字」：`sample_count` 会变成 3，
> 页面显示成「本次新采集入库 4 条」。本项目在实体按键的事件 id 上已经栽过一次同样的坑，
> 这是第二次 —— 两次的共同点是：**重复/冲突的标识符被当成了新事实**。

### 7.3 重复点击（任务卡要求「测试重复点击」）

**策略：同一台设备的同类指令，同一时刻只允许一条在途。** 有在途的就返回 **409**，
并带上那条指令的 `request_id`。

为什么必须这样：连点 5 次若照单全收，服务器会排队 5 条，而设备 1 秒只能取走 1 条 ——
最后一条要在几十秒后才执行（那时用户早走开了），前面几条还会撞上 `accept` 超时，
页面上呈现出一片「完成 / 超时」混杂，看起来像 bug。

实测（`tools/dup_click_test.py`，**5 个请求并发发出**，模拟两个标签页/多台手机一起点）：

| 检查项 | 结果 |
|---|---|
| 受理 / 拒绝 | **恰好 1 条 202、4 条 409** |
| 拒绝指向哪条 | 4 条**都指向同一条**在途指令（不是各说各话） |
| 落盘核对 | 库里在途的 `recollect` **恰好 1 条** —— 没有偷偷排队 |
| 异类指令 | 采集在途时点「暂停」→ **202 放行**（不同类互不阻塞） |
| 闸门释放 | 在途那条走完后再点 → **202 受理** |
| 浏览器侧 | 在页面上下文里并发发两个请求 → **1 个 202 + 1 个 409** |

两个设计取舍值得写下来：

- **闸门按「同类」判定，不按设备全局判定。** 把暂停一起堵住会很难解释，也会挡住该做的事。
- **闸门必须在服务器。** 界面上的按钮 `disabled` 只能管住自己那个标签页，
  拦不住"两个浏览器同时点" —— 这正是浏览器侧那条检查存在的意义。
- **网页收到 409 不弹红字了事**，而是直接接到那条在途指令上继续跟踪。
  用户真正在意的不是"被拒绝了"，而是"我这一下到底有没有生效"。

### 7.4 首版状态图（任务卡要求交付物）

见 [`docs/design/10-指令状态机与状态图.md`](../docs/design/10-指令状态机与状态图.md)。
包含：六态定义与谁能推进、`failed` 与 `timeout` 为什么必须分开、
三种触发来源（`timer`/`command`/`button`）各自有没有状态机、
并发闸门与幂等规则、以及六条已知限制。

其中一条值得在这里重复：**实体按键没有下行通道**，所以它**不存在**「服务器受理」和
「指令下发」两步 —— 网页上给它画四阶段时间线就是伪造证据，因此只列真实发生的事实。

## 八、复现方式

```bash
# 1) 启动服务器
python week1/server/server.py

# 2) 真机验收（下发指令 + 打印完整证据链）
cd week1
python tools/cmd_acceptance.py --samples 12 --interval-ms 100 --save

# 3) 无硬件时的服务器自测（软件模拟板子，含超时分支）
python tools/cmd_selftest.py --timeout-test

# 4) 网页操作：浏览器打开 http://127.0.0.1:8000
#    点「重新采集」→ 上方时间线会依次点亮四个阶段，
#    底部显示「下发前 N 条 → 下发后 M 条」，
#    表格里带「指令」标记的行就是本次新采的

# 5) 真实浏览器自动实测（自动点击按钮 + 截图）
python tools/browser_test.py

# 6) 暂停周期上报验收（会真的暂停/恢复真板子）
python tools/pause_acceptance.py

# 7) 重复点击 / 失败态（服务器级，不用硬件；跑完自动清理测试数据）
python tools/dup_click_test.py --clicks 5

# 8) 失败路径真机验收 —— 需要服务器带注入口启动
python week1/server/server.py --fault-injection      # 换这个方式起
python tools/fail_acceptance.py --samples 6 --interval-ms 120 --save

# 9) 网页能否显示「失败」的真实浏览器实测（同样需要 --fault-injection）
python tools/browser_test_fail.py
```

> `--fault-injection` 是一个**测试专用**开关：打开后可用
> `POST /api/debug/fault` 人为让设备上报失败，用于验证 `failed` 这条路径。
> 默认关闭，且启动横幅会明确打印「⚠️ 故障注入已开启」——
> 能人为制造故障的开关，忘了关必须一眼看得见。

---

## 九、与单元1 评价证据的对照

| 课程要求 | 本项目的证据 |
|---|---|
| 板端 / 服务端 / Web 工程 | `firmware/esp32_imu/`、`server/`、`web/index.html` |
| 真实观测与页面对应 | 本记录第二、四节；数据来自 `src_ip=10.1.41.160` 的真机 |
| **请求 — 回执 — 新观测记录** | 第二、三、四节：一条 `request_id` 贯穿受理→下发→接收→完成→13 条新记录 |
| 服务器受理 / 设备接收 / 完成 / 超时的证据链 | 第二节（四段齐全）+ 第五节（超时并记录阶段） |
| Web 显示**完成 / 失败 / 超时** | 完成：第二节；超时：第五节；**失败：第七节 7.2**（真机注入故障验证，含对照实验） |
| 数据值 / 来源 / 采集时间对应 | 第四节的表：每一项都带服务器时间与来源 IP；模长 0.99~1.01g 符合物理预期 |
| **刷新存储记录 vs 触发新采集** | 第三节：刷新增 0 条，指令增 13 条；**第七节 7.1**：暂停周期上报后新记录 100% 来自指令 |
| **暂停周期上报、保持命令通道** | 第七节 7.1：6 秒 0 条新增，但心跳 2.01 秒前仍在 |
| **测试重复点击** | 第七节 7.3：5 个并发请求 → 恰好 1 个受理 + 4 个 409 |
| **首版状态图** | [`docs/design/10-指令状态机与状态图.md`](../docs/design/10-指令状态机与状态图.md) |
| 个人修改与运行说明 | `README.md`、`docs/design/08-下行指令与回执状态机.md` |

---

## 十、遗留与下一步

- **本次核对暴露的方法论问题**：以上四项之所以漏掉，是因为我拿**自己写的任务摘要**
  去验收，而那份摘要是压缩过的 —— 没读懂、没做的部分连同摘要一起被压掉了。
  已把任务卡原文逐条存进项目长期笔记，并定了一条硬规矩：
  **任何"完成"结论必须对着任务卡原文逐条打勾，明确标出未做/部分做。**
- **服务部署**：课程允许"没有 VPS 就用自己的电脑当服务器"，目前**已按此跑通**——
  服务器跑在 `10.1.41.18:8000`（监听 `0.0.0.0`），板子通过局域网上报，
  局域网内任意设备都能打开页面。实测与差距清单见
  [`部署说明-用电脑当服务器.md`](部署说明-用电脑当服务器.md)。
  **但"外出远程访问"还没解决**：需要公网 IP / 端口映射 / 内网穿透之一，
  且摄像头要改成板子主动推流（VPS 在公网时，服务器拉不到 NAT 后面的板子）。
- **板子侧 HTTP 往返偏慢（实测发现，未解决）**：从板子自己的串口日志看，
  一次 `POST /api/data` 要 0.5~2.5 秒（同一批指令里逐条波动），期间还有一次直接
  `HTTP -1`（请求根本没完成）。它解释了此前所有偏大的耗时数字
  （端到端 4~13 秒、按下→采样 0.4~1.2 秒），**不是代码算得慢，是网络往返慢**。
  可能的方向：给 `esp_http_client` 开 keep-alive（现在每条都新建 TCP 连接，
  每次都要重新握手），但那需要单独验证，本轮没有动。
- **第3周**：板载实体按键触发采集 + 本地（LED/蜂鸣器）/ 远端（Web）物理反馈闭环。
- 按项目总目标，传感器还应从「IMU + 图像」扩展到 3~4 种（**声音**最现成：
  板载数字 I2S MEMS 麦克风 61dB SNR，尚未使用），再谈多模态融合与端云模型协同。
