# 第 3 周验收记录 · 实体按键与本地/远端反馈闭环

日期：2026-09-20　设备：ESP32-S3-EYE（`team01-esp32s3eye`，`10.1.41.160`）
服务器：`10.1.41.18:8000`（用本机代替 VPS）　固件：`esp32_imu.bin` 1006 KB

---

## 一、这一周要证明什么

课程目标是"板载实体按键触发采集 + 本地（LED）/远端（Web）反馈闭环"。拆成三件可验证的事：

| 要证明的事 | 证据在哪 |
|---|---|
| ① 板子上的按键真的能触发新采集 | 数据库里出现 `trigger='button'` 的新记录，条数与时间都对得上 |
| ② 本地有物理反馈 | 按下去绿灯会亮（**只能靠人眼**，见第五节） |
| ③ 远端能看到这次操作 | 网页「实体交互」面板出现该事件（真实浏览器实测） |

---

## 二、硬件核验（动手前先查清楚，不猜）

### 2.1 四个按键的实际电压

原理图标注 `4-Keys: ADC1_CH0`，即四个键共用 **GPIO1**，靠分压电压区分。
工具：`tools/btn_probe.py`（串口侧，需要人按键）

| 按键 | 原理图设计值 | 第 1 次实测 | 第 2 次实测 | 判决边界 | 归类 |
|---|---|---|---|---|---|
| UP+ | 380 mV | 367 mV | 365 mV | 0 ~ 600 | ✓ |
| DN− | 820 mV | 802 mV | 800 mV | 600 ~ 1400 | ✓ |
| PLAY | 1980 mV | 1981 mV | 1981 mV | 1400 ~ 2195 | ✓ |
| MENU | 2410 mV | 2409 mV | 2414 mV | 2195 ~ 2855 | ✓ |
| 无人按键 | ≈3300 mV | **3162 mV** | 3162 mV | >2855 → 无键 | ✓ |

最大偏差 −20 mV，远小于最小档间距（430 mV），**设计值可直接用于判决**。

```
空闲电压 3162 mV → 判读 无按键
UP+   实测  365 mV （原理图 380 mV，偏差 -15 mV）
DN-   实测  800 mV （原理图 820 mV，偏差 -20 mV）
PLAY  实测 1981 mV （原理图 1980 mV，偏差  +1 mV）
MENU  实测 2414 mV （原理图 2410 mV，偏差  +4 mV）
结论: 四个键的实测电压都落在各自判决区间内 ✓
```

### 2.2 本地反馈只能用 LED

- 本板**没有蜂鸣器**（只有输入用的麦克风），**没有可编程 RGB 灯**
- 唯一软件可控的发光器件是绿色 Module Power LED，接 **GPIO3**
- 官方指南要求 **开漏输出、不得上拉**，否则可能烧灯 —— 固件按此实现

**人眼确认结果**：重启板子时绿灯**连闪 3 次**（上电自检）✓
`docs/design/09` 第 1.3 节列出了四种灯效的含义。

---

## 三、真机证据链

### 3.1 修复后固件（2026-09-20 14:58，四次按键）

| 事件 id | 按键 | 新采集 | 记录 id | 入库跨度 | 按下→首条采样 |
|---|---|---|---|---|---|
| `BTN-2a3613-UP+-0001` | UP+ | **5 条** | 26153 ~ 26157 | 1608 ms | 471 ms |
| `BTN-2a3613-DN--0002` | DN− | **5 条** | 26160 ~ 26164 | 1115 ms | 1221 ms |
| `BTN-2a3613-MENU-0003` | MENU | **5 条** | 26167 ~ 26171 | 968 ms | 1061 ms |
| `BTN-2a3613-PLAY-0004` | PLAY | **5 条** | 26175 ~ 26179 | 893 ms | 691 ms |

四个键都识别正确、每次都恰好 5 条、跨度都在 1~2 秒内。

**「按下→首条采样」为什么是几百毫秒到 1 秒多？** 因为主循环正在等上一次
HTTP 上报的响应（超时上限 2 s），按键要等它回来才轮得到。
这个数字如实反映了当前架构的代价，见 `docs/design/09` 第 2.3 与第 6 节。

### 3.2 三种触发方式在同一条数据上可区分

```
库内总数 25213 条
  timer   : 4657 条   ← 板子自己 1Hz 上报
  command :   67 条   ← 网页点「重新采集」下发指令产生
  button  :   65 条   ← 板载实体按键产生
```

`trigger` / `request_id` / `button` 三个字段一起，让"这条数据是谁触发的"
成为**数据库里的直接关联**，而不是靠时间窗口猜。

### 3.3 一次按键与一次刷新的区别

| 动作 | 服务器行为 | 库内条数 | 新数据的标记 |
|---|---|---|---|
| 刷新网页 | 只读 SQLite | **不变** | — |
| 点「重新采集」 | 受理指令、等设备执行 | +5 ~ +13 | `trigger='command'` + `request_id='CMD-…'` |
| **按板上的键** | 设备**主动**上报 | **+5** | `trigger='button'` + `button='MENU'` |

注意第三行与前两行有个本质差别：**按键没有下行通道** —— 没有"服务器受理"
和"指令下发"两步。所以网页上给按键事件画四阶段时间线会是伪造证据，
面板里刻意只列真实发生的事。

---

## 四、真实浏览器实测

工具：`tools/browser_test_btn.py`（Chrome 153 无头 + DevTools Protocol，只读校验）

```
[2] 「实体交互」面板
    面板存在      : ✓
    标题          : ✓ 找到
    右上角计数    : 最近 6 次 · 最后一次 40 秒前
    事件行数      : 6
       · PLAY 2026-09-20 14:58:20 新采集 5 条 记录 id 26175 ~ 26179 入库跨度 893 ms 按下→采样 691 ms 40 秒前 BTN-2a3613-PLAY-0004
       · MENU 2026-09-20 14:58:16 新采集 5 条 记录 id 26167 ~ 26171 入库跨度 968 ms 按下→采样 1061 ms 44 秒前 BTN-2a3613-MENU-0003
       · DN-  2026-09-20 14:58:12 新采集 5 条 记录 id 26160 ~ 26164 入库跨度 1115 ms 按下→采样 1221 ms 48 秒前 BTN-2a3613-DN--0002
       · UP+  2026-09-20 14:58:08 新采集 5 条 记录 id 26153 ~ 26157 入库跨度 1608 ms 按下→采样 471 ms 51 秒前 BTN-2a3613-UP+-0001

[4] 与 /api/buttons 对账
    接口 6 条（取前 6）vs 页面 6 行  ✓ 一致
    最新事件 BTN-2a3613-PLAY-0004 是否出现在页面首行: ✓
结论: ✓ 面板在真实浏览器中渲染正确，且与接口数据一致
```

截图：`debug_logs/_w3_browser_btn.png`

---

## 五、无硬件自测（换台机器也能复现）

`tools/btn_selftest.py` 用软件模拟板子按键，不需要任何硬件：

```
[4] ✓ 找到本次事件：按键=MENU 样本=5 记录 id 25467~25471 跨度=437ms
    定时上报是否混入按键事件: 否 ✓
[5] 指令表里是否出现按键事件 id: 否 ✓
结论: 全部通过 ✓  按键链路（上报 → 聚合 → 与定时/指令隔离）正常
```

它专门验证三个容易出错的地方：聚合是否正确、定时上报会不会混进来、
按键批次会不会污染指令状态机。

---

## 六、过程中发现并修复的两个真问题

### 6.1 事件 id 跨重启重复，导致两次按键被合并（严重）

**现象**：第一次真机测试得到的事件，样本数 10、入库跨度 104839 ms —— 都不对。

```
BTN-UP+-0001   UP+  样本=10  id 25839~25941  跨度=107352 ms   ← 应该是 5 条 / 约 2 秒
```

**根因**：事件 id 用的是内存里的计数器 `s_btn_n`，**板子一重启就归零**。
两次不同时间按的 UP+ 生成了同一个 id，服务器按 id 聚合时把两次按合成了同一条。

**修复**：给事件 id 加**上电随机前缀**（24 位）：

```
修复前: BTN-UP+-0001
修复后: BTN-2a3613-UP+-0001      ← 2a3613 = esp_random() & 0xFFFFFF，每次上电不同
```

**为什么这件事值得单独记**：它产出的不是"没有数据"，而是**看起来像证据、
实际是错的**数据。样本数 10、跨度 105 秒，如果不去核对，会直接被写成
"一次按键采了 10 条、耗时 105 秒"。**错的证据比没有证据更危险。**

**顺带加的防线**：网页现在会把跨度 > 30 秒的事件标红提示
「跨度异常 · 事件 id 可能撞车」—— 让这类问题在界面上自己暴露，而不是靠人盯着数字发现。
修复前的 4 条旧事件仍保留在库里，正好当作这个告警的实物样本。

### 6.2 串口工具把板子复位了，导致 200 秒采集空白

**现象**：第一版 `btn_probe.py` 打开 COM5 后，200 秒里**一个字节都没收到**，
但板子其实活着（服务器一直在收数据）。

**根因**：pyserial 打开串口时默认拉高 DTR/RTS，而 ESP32-S3 上这两个信号的组合
会触发芯片复位。于是"打开串口"这个动作本身把板子重启了；芯片重启 → USB 重新枚举
→ Windows 下**旧句柄静默失效**（不报错，只是再也读不到数据）。

**修复**：打开后显式 `setDTR(False)` / `setRTS(False)`，并加一个看门狗 ——
连续 5 秒读不到数据就关掉重开串口。

**教训**：调试工具本身也会改变被测对象的状态。**"观测不到"要先怀疑观测手段，
再怀疑被测对象。**

---

## 七、复现方式

```bash
# 0) 启动服务器（会打印局域网地址，板子靠它上报）
cd D:\aijiaohu\week1\server
python server.py --port 8000 --host 0.0.0.0

# 1) 不需要硬件：验证按键链路（上报 → 聚合 → 与定时/指令隔离）
cd D:\aijiaohu\week1
python tools\btn_selftest.py

# 2) 需要硬件：核验四个键的实际电压（用 ESP-IDF 自带的 python，含 pyserial）
D:\gongju\Espressif\python_env\idf5.4_py3.11_env\Scripts\python.exe ^
    tools\btn_probe.py --port COM5 --seconds 120 --raw debug_logs\_btn_raw.txt

# 3) 需要人：真机验收（脚本只等待、只观察，没人按就如实报"没等到"）
python tools\btn_acceptance.py

# 4) 真实浏览器校验面板渲染
python tools\browser_test_btn.py
```

---

## 八、与单元1 评价证据的对照

| 评价要求 | 本项目的证据 |
|---|---|
| 实体操作 | 四个键的实测电压表（第 2.1 节）+ 真实按键事件（第 3.1 节） |
| 本地反馈 | 绿灯自检闪 3 次（人眼确认）+ 四种灯效语义（`docs/design/09` 1.3） |
| 远端反馈 | 网页「实体交互」面板 + 真实浏览器实测（第 4 节） |
| 请求—回执—新观测记录 | `trigger='button'` + `button='MENU'` + `request_id='BTN-…'` 三字段关联 |
| 数据值 / 来源 / 采集时间对应 | 每条记录带服务器时间与来源 IP `10.1.41.160`；模长 0.99~1.01 g |
| 真实观测与页面对应 | 页面数字与 `/api/buttons` 逐字段对账一致（第 4 节 `[4]`） |
| 个人修改 | 完整设计文档 `docs/design/09`、两个排错案例（第 6 节） |

---

## 九、遗留与下一步

- **本地反馈手段单一**：只有一颗单色灯，灯效种类有限（亮度/颜色都不可调）。
  子板上的 1.3" LCD 尚未使用，若驱动起来可以做更丰富的本地反馈。
- **按键响应受 HTTP 阻塞**：`press_delay_ms` 在 0.4~1.2 秒之间波动。
  根治要把 HTTP 挪到独立任务、用队列串起来。
- **外出远程访问仍未完成**：课程要求，目前只有局域网可达。
- 第 3 周完成后仓库应新增 `week3` 分支，并把默认分支推进到它。
